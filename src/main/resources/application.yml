spring:
  application:
    name: "university-console"
  datasource:
    url: "jdbc:postgresql://localhost:5432/postgres"
    password: "1111"
    username: "postgres"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true